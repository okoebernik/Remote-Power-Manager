CREATE DATABASE IF NOT EXISTS remote_power_manager
    CHARACTER SET utf8mb4
    COLLATE utf8mb4_unicode_ci;

USE remote_power_manager;

-- Die Tabellen werden von der Anwendung automatisch angelegt.
